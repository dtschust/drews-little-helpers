#!/usr/bin/env node

const doTasks = require('../src/do-tasks');

const tasks = [
	'checkUpdatesForMomo',
	'letterboxdToRSS',
	'restoreFeedsOnHiatus',
	'ptpTopWeekly hour',
	'clearOldCronLogs',
];

async function main() {
	await doTasks(tasks, 'Hourly');
}

main().then(process.exit);
