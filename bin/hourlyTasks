#!/usr/bin/env node

const loadDist = require('./load-dist');
const doTasks = loadDist('src/utils/do-tasks.js');

const tasks = [];

async function main() {
	await doTasks(tasks, 'Hourly');
}

main().then(process.exit);
